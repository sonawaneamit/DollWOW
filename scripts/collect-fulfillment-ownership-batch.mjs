import fs from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const ROOT = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const API_BASE = "https://api.dataforseo.com/v3";
const execute = process.argv.includes("--execute");
const fetchMerchant = process.argv.includes("--fetch-merchant");
const outputDir = path.join(ROOT, "data/exports/seo-intelligence/2026-08-12/step-78-fulfillment-ownership-batch");

await loadEnv(path.join(ROOT, ".env.local"));
if (execute && (!process.env.DATAFORSEO_LOGIN || !process.env.DATAFORSEO_PASSWORD)) throw new Error("DataForSEO credentials are required.");

const topics = [
  topic("shipping", "discreet sex doll shipping", ["discreet sex doll shipping", "sex doll shipping", "sex doll delivery", "sex doll discreet packaging", "sex doll delivery time"], "https://dollwow.com/learn/discreet-sex-doll-shipping", "What should a US buyer verify about discreet sex doll shipping before checkout? Cover outer packaging, label, billing, messages, stock versus production, tracking, carrier handoff, signature, apartments, customs, arrival inspection, damage reporting, privacy limits, and support. Cite current sources.", ["https://www.yourdoll.com/pages/shipping-policy", "https://www.rosemarydoll.com/pages/shipping-policy", "https://www.sexdollqueen.com/pages/shipping-policy"]),
  topic("order-path", "ready to ship vs custom sex dolls", ["ready to ship sex doll", "in stock sex doll", "custom sex doll", "made to order sex doll", "ready to ship vs custom sex dolls"], "https://dollwow.com/learn/ready-to-ship-vs-custom-sex-dolls", "How should a US buyer choose between a ready-to-ship and made-to-order sex doll? Compare exact unit, options, compatibility, price, production, Human Build Check, eligible factory media, dispatch, delivery, arrival inspection, support, and which buyer each path fits. Cite current sources.", ["https://www.yourdoll.com/collections/us-warehouse", "https://www.rosemarydoll.com/collections/us-warehouse", "https://www.sexdollqueen.com/collections/usa-stock"]),
  topic("maintenance", "sex doll maintenance", ["sex doll maintenance", "sex doll care", "how to care for a sex doll", "sex doll maintenance checklist", "tpe sex doll maintenance"], "https://dollwow.com/learn/sex-doll-maintenance-checklist", "What maintenance routine should a sex doll owner follow? Cover pre-purchase handling and storage fit, cleaning, complete drying, inspection, TPE and silicone boundaries, stains, pressure, joints, standing feet, electronics, hair, supplies, repair triage, record keeping, and when to ask support. Cite current manufacturer sources.", ["https://www.tantaly.com/pages/cleaning-and-maintenance-guide-for-sex-doll", "https://realdoll.com/wp-content/uploads/2025/08/Care-Guide-Realdoll-Classic-R2.pdf", "https://www.zelexdoll.com/pages/dolls-guide"])
];

const keywords = [...new Set(topics.flatMap((item) => item.keywords))];
const calls = [
  call("bing-keywords", "Bing Keyword Data", "/keywords_data/bing/search_volume/live", [{ keywords, location_code: 2840, language_code: "en", tag: "dollwow-fulfillment-ownership-bing" }]),
  call("ai-keywords", "AI Keyword Data", "/ai_optimization/ai_keyword_data/keywords_search_volume/live", [{ keywords, location_code: 2840, language_code: "en", tag: "dollwow-fulfillment-ownership-ai-keywords" }]),
  ...topics.flatMap((item) => [
    call(`bing-serp-${item.key}`, "Bing SERP", "/serp/bing/organic/live/advanced", [{ keyword: item.keyword, location_code: 2840, language_code: "en", device: "desktop", os: "windows", depth: 20, tag: `dollwow-fulfillment-ownership-bing-${item.key}` }]),
    call(`ai-mode-${item.key}`, "Google AI Mode", "/serp/google/ai_mode/live/advanced", [{ keyword: item.prompt, location_code: 2840, language_code: "en", device: "desktop", tag: `dollwow-fulfillment-ownership-ai-mode-${item.key}` }]),
    call(`chatgpt-${item.key}`, "ChatGPT Search", "/ai_optimization/chat_gpt/llm_scraper/live/advanced", [{ keyword: item.prompt, location_code: 2840, language_code: "en", tag: `dollwow-fulfillment-ownership-chatgpt-${item.key}` }]),
    call(`claude-${item.key}`, "Claude", "/ai_optimization/claude/llm_responses/live", [{ user_prompt: item.prompt, system_message: "Answer as a neutral US buyer researcher. Prefer current official and verifiable sources.", model_name: "claude-haiku-4-5", max_output_tokens: 1400, web_search: true, tag: `dollwow-fulfillment-ownership-claude-${item.key}` }]),
    call(`gemini-${item.key}`, "Gemini", "/ai_optimization/gemini/llm_responses/live", [{ user_prompt: item.prompt, system_message: "Answer as a neutral US buyer researcher. Prefer current official and verifiable sources.", model_name: "gemini-2.5-flash-lite", max_output_tokens: 1400, web_search: true, tag: `dollwow-fulfillment-ownership-gemini-${item.key}` }]),
    call(`perplexity-${item.key}`, "Perplexity", "/ai_optimization/perplexity/llm_responses/live", [{ user_prompt: item.prompt, system_message: "Answer as a neutral US buyer researcher. Prefer current official and verifiable sources.", model_name: "sonar", max_output_tokens: 1400, web_search_country_iso_code: "US", tag: `dollwow-fulfillment-ownership-perplexity-${item.key}` }]),
    call(`content-${item.key}`, "Content Analysis", "/content_analysis/search/live", [{ keyword: item.keyword, limit: 30, order_by: ["content_info.date_published,desc"], tag: `dollwow-fulfillment-ownership-content-${item.key}` }]),
    call(`backlinks-${item.key}`, "Backlinks", "/backlinks/page_intersection/live", [{ targets: Object.fromEntries(item.competitors.map((url, index) => [index + 1, url])), exclude_targets: [item.url], intersection_mode: "all", limit: 100, order_by: ["1.rank,desc"], backlinks_status_type: "live", include_subdomains: true, exclude_internal_backlinks: true, rank_scale: "one_hundred", tag: `dollwow-fulfillment-ownership-backlinks-${item.key}` }]),
    call(`onpage-${item.key}`, "OnPage", "/on_page/instant_pages", [{ url: item.url, load_resources: false, enable_javascript: false, check_spell: false }])
  ]),
  ...["google", "chat_gpt"].map((platform) => call(`mentions-${platform}`, "LLM Mentions", "/ai_optimization/llm_mentions/target_metrics/live", [{ location_code: 2840, language_code: "en", platform, target: [{ keyword: "dollwow.com", search_scope: ["any"], match_type: "word_match" }, ...topics.map((item) => ({ keyword: item.keyword, search_scope: ["any"], match_type: "word_match" }))], tag: `dollwow-fulfillment-ownership-mentions-${platform}` }]))
];

await fs.mkdir(outputDir, { recursive: true });
const records = [];
if (!fetchMerchant) for (const item of calls) records.push(await executeCall(item));
if (!fetchMerchant) for (const item of topics) records.push(await executeMerchant(item));
if (fetchMerchant) for (const item of topics) records.push(await fetchMerchantResult(item));
const manifest = { generatedAt: new Date().toISOString(), execute, totalCost: records.reduce((sum, item) => sum + Number(item.cost || 0), 0), requestCount: records.length, failedRequests: records.filter((item) => item.error).length, records };
await fs.writeFile(path.join(outputDir, fetchMerchant ? "request-manifest-merchant-results.json" : "request-manifest.json"), `${JSON.stringify(manifest, null, 2)}\n`);
console.log(JSON.stringify({ outputDir: path.relative(ROOT, outputDir), totalCost: manifest.totalCost, requestCount: manifest.requestCount, failedRequests: manifest.failedRequests }, null, 2));

function topic(key, keyword, keywordList, url, prompt, competitors) { return { key, keyword, keywords: keywordList, url, prompt, competitors }; }
function call(id, api, endpoint, payload) { return { id, api, endpoint, payload }; }
async function executeCall(item) { const record = { ...item, executed: execute, cost: 0, statusCode: null, statusMessage: execute ? null : "Dry run" }; if (!execute) return record; try { const response = await post(item.endpoint, item.payload); const task = response.tasks?.[0] || {}; Object.assign(record, { taskId: task.id || null, cost: Number(task.cost || 0), statusCode: task.status_code || response.status_code || null, statusMessage: task.status_message || response.status_message || null }); await fs.writeFile(path.join(outputDir, `${item.id}.json`), `${JSON.stringify(response, null, 2)}\n`); } catch (error) { record.error = true; record.statusMessage = error instanceof Error ? error.message : String(error); } return record; }
async function executeMerchant(item) { const id = `merchant-${item.key}`; const record = { id, api: "Merchant", executed: execute, cost: 0, statusCode: null, statusMessage: execute ? null : "Dry run" }; if (!execute) return record; try { const response = await post("/merchant/google/products/task_post", [{ keyword: item.keyword, location_code: 2840, language_code: "en", depth: 40, priority: 2, tag: `dollwow-fulfillment-ownership-${id}` }]); const task = response.tasks?.[0] || {}; Object.assign(record, { taskId: task.id || null, cost: Number(task.cost || 0), statusCode: task.status_code || response.status_code || null, statusMessage: task.status_message || response.status_message || null }); await fs.writeFile(path.join(outputDir, `${id}-post.json`), `${JSON.stringify(response, null, 2)}\n`); } catch (error) { record.error = true; record.statusMessage = error instanceof Error ? error.message : String(error); } return record; }
async function fetchMerchantResult(item) { const id = `merchant-${item.key}`; const posted = JSON.parse(await fs.readFile(path.join(outputDir, `${id}-post.json`), "utf8")); const taskId = posted.tasks?.[0]?.id; const record = { id: `${id}-result`, api: "Merchant", executed: execute, cost: 0, statusCode: null, statusMessage: execute ? null : "Dry run" }; if (!execute) return record; try { const response = await fetch(`${API_BASE}/merchant/google/products/task_get/advanced/${taskId}`, { headers: authHeaders() }); const body = await response.json(); const task = body.tasks?.[0] || {}; Object.assign(record, { statusCode: task.status_code || body.status_code || null, statusMessage: task.status_message || body.status_message || null }); await fs.writeFile(path.join(outputDir, `${id}.json`), `${JSON.stringify(body, null, 2)}\n`); } catch (error) { record.error = true; record.statusMessage = error instanceof Error ? error.message : String(error); } return record; }
async function post(endpoint, payload) { const response = await fetch(`${API_BASE}${endpoint}`, { method: "POST", headers: authHeaders(), body: JSON.stringify(payload) }); const body = await response.json(); const failed = body.tasks?.find((task) => Number(task.status_code || 0) >= 40000); if (!response.ok || Number(body.status_code || 0) >= 40000 || failed) throw new Error(failed?.status_message || body.status_message || `HTTP ${response.status}`); return body; }
function authHeaders() { return { Authorization: `Basic ${Buffer.from(`${process.env.DATAFORSEO_LOGIN}:${process.env.DATAFORSEO_PASSWORD}`).toString("base64")}`, "Content-Type": "application/json" }; }
async function loadEnv(file) { const text = await fs.readFile(file, "utf8"); for (const line of text.split(/\r?\n/)) { const trimmed = line.trim(); if (!trimmed || trimmed.startsWith("#")) continue; const equals = trimmed.indexOf("="); if (equals < 1) continue; const key = trimmed.slice(0, equals).trim(); const value = trimmed.slice(equals + 1).trim().replace(/^['"]|['"]$/g, ""); if (!(key in process.env)) process.env[key] = value; } }
